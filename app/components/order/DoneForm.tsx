import React from 'react';
import { SessionUserData } from '~/types';

export default function DoneForm(props: { setOpts: Function; curUser: SessionUserData}) {
  return (
    <div>DoneForm</div>
  );
}
