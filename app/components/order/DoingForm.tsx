import React from 'react';
import { SessionUserData } from '~/types';

export default function DoingForm(props: { setOpts: Function; curUser: SessionUserData}) {
  return (
    <div>DoingForm</div>
  );
}
