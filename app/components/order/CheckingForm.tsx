import React from 'react';
import { SessionUserData } from '~/types';

export default function CheckingForm(props: { setOpts: Function; curUser: SessionUserData}) {
  return (
    <div>CheckingForm</div>
  );
}
